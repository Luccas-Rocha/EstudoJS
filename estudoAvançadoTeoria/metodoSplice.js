const names = ['maria', 'joão', 'eduardo', 'gabriel', 'julia']

//names.splice(começo, deletar, elem1,elem2,elem3);   // retorna um array
//names.pop(); = names.splice(-1, 1) 
//names.shift(); = names.splice(0, 1)
//names.push(); = names.splice(nomes.lenght, 0, 'joao')
//names.unshift(); = names.splice(0, 0, 'joao');
//names.splice(3, 0, 'Luiz');           //adiciona elementos no indice 3
console.log(names);
